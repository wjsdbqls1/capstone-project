# 개발 서버(npm run dev)가 바라볼 백엔드 주소.
# 기존 동작(로컬 개발도 운영 서버를 호출)을 그대로 유지한다.
# 백엔드를 로컬에서 띄워 개발하려면 아래 두 줄을 서로 바꿔 주면 된다.
VITE_API_BASE=https://capstone-project-of74.onrender.com
# VITE_API_BASE=http://localhost:8000

# 배포(npm run build) 시에는 .env.production이 우선하므로 이 파일은 영향을 주지 않는다.
