.venv/

# 서버 비밀값(DB 접속 정보, JWT 시크릿, Supabase 키)이 들어가는 파일.
# 프론트의 .env는 공개 API 주소만 담고 있어 이미 커밋되어 있다.
backend/.env
backend/.env.*
.env.local

# 학생 개인정보(학번·이름)가 들어 있는 파일. 한 번 커밋되면 히스토리에 남으므로
# 실수로도 올라가지 않게 확장자 단위로 막는다.
*.xlsx
*.xls
backup_*.json

__pycache__/
*.pyc
*.pt
*.pth
backend/uploads/
